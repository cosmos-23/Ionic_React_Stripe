import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_react_stripe',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
